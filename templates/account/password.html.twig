{% extends 'base.html.twig' %}

{% block title %}Modify password - Hypnos{% endblock %}

{% block content %}
<div class="container">
    <h1>My account</h1>
    <p> Bienvenue {# {{ app.user.firstname }} #}</p>
    <a href="{{ path('account') }}">Account</a>
    <hr>
    {% if notification %}
    <div class="alert alert-info">{{ notification }}</div>
    {% endif %}
    {{form(form)}}
</div>


{% endblock %}